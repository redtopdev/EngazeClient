package com.redtop.engaze.interfaces;

import com.google.android.gms.location.places.Place;

public interface OnGetCurrentLocationCompleteListner {
	void OnGetCurrentLocationComplete(Place currentPlace);

}
